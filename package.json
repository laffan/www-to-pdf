{
  "name": "www-to-pdf",
  "version": "0.1.0",
  "description": "Capture a website, clean it up, and save it as a PDF — web app + native app.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify-editor.mjs",
    "tauri": "tauri",
    "app:dev": "tauri dev",
    "app:build": "tauri build",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev",
    "build:ios": "tauri ios build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "playwright": "^1.61.1",
    "vite": "^6.0.0",
    "pdfjs-dist": "3.11.174"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0"
  }
}
